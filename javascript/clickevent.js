"use strict";

function getDetails() {
    var username = document.querySelector("#username").value;
    var userpassword = document.querySelector("#userpassword").value;

    if (username == "") {
        document.querySelector(".errormsg").no
    }
}